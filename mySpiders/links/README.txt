Info:
    This spider can scrapy the titles, time and links, 
    and automatically scrapy next page util the end.

    The file spider.py is the main spider, linkSpider.py is my test spider.

Usage:
    Use command `scrapy crawl alls` to write the info into a file.

